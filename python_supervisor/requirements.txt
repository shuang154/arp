# ARP Spoofer C++ Core - 最小化Python依赖
# ====================================
# 性能优化版本：关闭Web功能，专注核心性能

# 基础依赖（必需）
PyYAML>=6.0                   # 配置文件解析
pybind11>=2.10.0              # Python-C++绑定

# Web API依赖（可选 - 性能优化时不安装）
# Flask>=2.0.0                # Web框架  
# flask-cors>=3.0.0           # CORS支持

# 注意：
# 1. 此版本专为性能优化，默认关闭Web API功能
# 2. 核心处理完全由C++负责，Python端极简化
# 3. 如需Web API功能，请取消注释Flask相关依赖
# 4. 香橙派系统依赖: sudo apt-get install libzmq3-dev libjsoncpp-dev python3-dev
